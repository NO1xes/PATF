# Weekly Report — 2026-05-14

**Stage**: MVP-0 Repository Skeleton

## What Was Done This Week

- Held group meeting; finalized AgentProf design (see `2026-05-13/agentprof_docs/`)
- Defined three-component architecture: Target Agent / LLM Backend / AgentProf Controller
- Defined four execution layers and cross-layer correlation mechanism
- Created GitHub private repo `NO1xes/AgentProf`
- Built full repository skeleton on local PC (Windows 11, no GPU)
- Wrote all required docs: README, AGENTS, PROJECT_STATUS, TODO, CHANGELOG, EXPERIMENTS, ENVIRONMENT, SECURITY
- Wrote all configs: profiling_spec, target_system (Qwen3-30B-A3B), observers, workload_controlled, machine config
- Wrote Python package stubs: schema, state, controller, timeline, report, questions, policy, all observers, storage
- Wrote controlled tools: slow_tool (2s sleep), cpu_tool (CPU loop), flaky_tool (fail-then-succeed)
- Wrote target agent stub (LangGraph ReAct)
- Wrote scripts: run_controlled_workload.sh, start_vllm.sh, verify_env.sh

## Current Breakdown

| Component | Status |
|---|---|
| Repo skeleton | Done |
| Controlled tools | Done (runnable) |
| Observer stubs | Stub only (Milestone 1) |
| Target agent | Stub only (Milestone 1) |
| Timeline / Report | Stub only (Milestone 2) |
| Controller loop | Stub only (Milestone 3) |
| vLLM backend | Pending GPU server |

## Known Unknowns

- Which GPU server will be used for vLLM? Who sets it up?
- Conda environment not yet created — pending first install attempt
- LangChain callback API compatibility with LangGraph needs verification

## Next Week Plan

1. Create conda env `agentprof` on local PC
2. Implement `semantic_langchain.py` and `llm_client_timing.py`
3. Implement `tool_wrapper.py`
4. Implement `storage/writer.py` (already done — verify it works)
5. Run slow_tool task against a remote API endpoint
6. Verify `events.jsonl` is produced correctly
